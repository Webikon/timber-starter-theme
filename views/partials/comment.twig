{# <?php #}

<div class="comment {{ comment.comment_type }}" id="comment-{{ comment.ID }}">
	<h4 class="comment__author">{{ comment.author.name }}</h4>
	<div class="comment__content">{{ comment.comment_content|wpautop }}</div>
</div>
